#include <cmath>
#include <iostream>
#include <iomanip> 
#include <basicStrategy.h>

using namespace std;

int main(void){

    double EV,betSize,potSize,WinPercentage;
    cout<<"Pot Size : ";
    cin >>potSize;
    cout<<endl<<"bet Size ";
    cin>>betSize;

    EV=expected_Value()
}